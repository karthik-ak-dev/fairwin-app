// Date utility functions
// Responsibilities:
// - formatDate(date): Format date for display
// - getMonthsElapsed(startDate, endDate): Calculate months difference
// - isFirstOfMonth(date): Check if date is 1st of month
// - getNextFirstOfMonth(): Calculate next withdrawal date
// - addMonths(date, months): Add months to date
// - Uses date-fns library
// - Used by reward calculations and withdrawal validation
