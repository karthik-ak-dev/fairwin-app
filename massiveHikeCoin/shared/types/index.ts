// Shared TypeScript type definitions
// Responsibilities:
// - Export all model interfaces (User, Stake, Referral, Withdrawal)
// - Define API response types
// - Define form state types
// - Define enum types (StakeStatus, WithdrawalStatus, etc.)
// - Ensure type consistency across frontend and backend
// - Used throughout application for type safety
