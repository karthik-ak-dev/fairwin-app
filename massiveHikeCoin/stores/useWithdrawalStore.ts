// Zustand store for withdrawal UI state
// Responsibilities:
// - Store withdrawal form state (amount, selectedStakes)
// - Store transaction status (pending, processing, completed)
// - Actions: setAmount(amount), selectStakes(stakeIds), setStatus(status), reset()
// - Used by withdrawal page
// - Client-side only state
