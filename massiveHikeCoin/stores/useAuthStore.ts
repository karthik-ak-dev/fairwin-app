// Zustand store for authentication state
// Responsibilities:
// - Store current user data (userId, walletAddress, isAdmin, token)
// - Store authentication status (isAuthenticated, isLoading)
// - Actions: login(user, token), logout(), setUser(user)
// - Persist token to localStorage
// - Provide getters for user info
// - Used throughout app for auth state
