// Wallet utilities for Web3 operations
// Responsibilities:
// - signMessage(message): Request wallet signature
// - verifySignature(message, signature, address): Verify signature
// - getWalletBalance(address): Get native token balance
// - switchToNetwork(chainId): Switch to BSC network
// - Uses wagmi and viem libraries
// - Used by authentication flow
