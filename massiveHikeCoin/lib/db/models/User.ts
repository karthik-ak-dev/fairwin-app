// User model (shared across platform)
// Responsibilities:
// - Define User interface with TypeScript types
// - Fields: userId (PK), walletAddress, depositWallet, withdrawalWallet, referredBy,
//   referralCode, createdAt, updatedAt, totalStaked, totalWithdrawn
// - Type definitions for user authentication and profile data
// - Validation rules for wallet addresses
// - This is a shared model used across the entire platform
