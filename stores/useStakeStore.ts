// Zustand store for stake UI state
// Responsibilities:
// - Store selected stake for detail view
// - Store stake form data (amount, referralCode)
// - Store calculator state (projected earnings)
// - Actions: setSelectedStake(stake), updateFormData(data), resetForm()
// - Used by stake and dashboard pages
// - Client-side only state (not persisted)
